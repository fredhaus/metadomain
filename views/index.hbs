<h1>{{title}}</h1>
<h1>Welcome {{#if user}} {{user.email}} {{else}} Stranger {{/if}} !</h1>

{{#if user}}
<a href="/logout">Log out</a>
{{else}}
<a href="/login">Log in</a>
<br>
<a href="/signup">Sign up</a>
{{/if}}
<form action="/result">
    <div class="form-group">
        <div class="search">
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="basic-addon1">www.</span>
                </div>
                <input type="text" class="form-control" name="domainSearch" placeholder="Your Domain"
                    aria-label="domainSearchQuery" aria-describedby="basic-addon1">
            </div>
        </div>
    </div>
    <button type="submit">New Search</button>
</form>